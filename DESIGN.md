# DESIGN.md — Сензитивность

Дизайн-система студии. Источник значений — реальный макет `reference/desktop.html`
(экспорт из Claude Design), а не выдуманные с нуля цифры. Токены живут в
`src/styles/tokens.css`. Вёрстка обязана использовать только их — ни одного
магического числа в компонентах.

## Палитра

| Токен | Hex | Назначение | Контраст (проверено) |
|---|---|---|---|
| `--color-ink` | `#22564D` | Первичный текст, заголовки | 7.76:1 на `--color-paper` — AA/AAA |
| `--color-ink-soft` | `#586B60` | Вторичный текст, подписи, тело абзаца | 5.27:1 на `--color-paper` — AA |
| `--color-mist` | `#8AA08F` | **Только декоративно**: обводки, разделители, неактивные иконки | 2.6–2.8:1 — недостаточно для текста, использовать как текст запрещено |
| `--color-moss` | `#0E6E5B` | Бренд/интерактив: кнопки, ссылки, иконки, фокус-кольцо | 5.72:1 на paper, 6.18:1 белый текст на moss — AA |
| `--color-moss-deep` | `#1B6E60` | Тёмные секции-баннеры, hover-состояние moss | 6.09:1 белый текст поверх — AA |
| `--color-lime` | `#CFE28C` | Единственный акцент: заливка CTA, активные состояния, крупные цифры на тёмном фоне | 5.95:1 текст `--color-ink` поверх — AA |
| `--color-paper` | `#F4F8E8` | Фон страницы — тёплый, не белый и не серый | — |
| `--color-surface` | `#FFFFFF` | Поверхность карточек поверх paper (не считается частью акцентной палитры) | — |

Правило: `--color-lime` — единственный акцент на весь сайт. Не вводить второй
акцентный цвет (никаких дополнительных синих/оранжевых/розовых пятен).
`--color-mist` не используется как цвет текста ни при каких обстоятельствах —
только обводки, декоративные штрихи, неактивные состояния.

## Типографика

Шрифты — те же, что в реальном фирменном макете студии (не Inter/Roboto/Arial,
не системный дефолт). Оба подтверждены как поддерживающие все нужные кириллические
начертания (обычное, полужирное, курсив) через Google Fonts API:

- `--font-display`: **Lora** (serif) — заголовки. Курсив используется для цитат.
- `--font-text`: **Golos Text** (sans-serif) — весь интерфейсный текст.

| Токен | Начертание | Где применяется |
|---|---|---|
| `--text-display` | Lora 600, 52px/1.14 | H1, только один на страницу |
| `--text-h2` | Lora 600, 40px/1.2 | Заголовки секций |
| `--text-h3` | Lora 600, 26px/1.25 | Подзаголовки внутри секции, крупные цифры-акценты |
| `--text-lead` | Golos Text 600, 19px/1.4 | Лид-абзацы, заголовки карточек программ |
| `--text-body` | Golos Text 400, 16.5px/1.6 | Основной текст, длина строки — 60–75 знаков |
| `--text-meta` | Golos Text 500, 14px/1.4 | Подписи, мета-данные (возраст, длительность, цена) |
| `--text-label` | Golos Text 700, 12.5px/1.3, uppercase | Эйбрау, лейблы-теги — всегда с `--tracking-label` |

Display и H2 используют `--tracking-display` (−0.02em) — плотный, уверенный
крупный заголовок. Лейблы используют `--tracking-label` (+0.04em) — читаемый
разрядённый капс на мелком кегле.

## Отступы

Шкала `--space-1…9` (4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96px) — кратна 4px,
с ускоренным ростом в крупных значениях под секционные отступы. Мелкие шаги
(1–4) — внутренние зазоры компонентов; крупные (6–9) — отступы между блоками
и секциями.

## Радиусы

| Токен | Значение | Где |
|---|---|---|
| `--radius-xs` | 8px | Мелкие теги, чекбоксы |
| `--radius-sm` | 14px | Инпуты, мелкие карточки |
| `--radius-md` | 20px | Карточки программ, отзывов, преимуществ |
| `--radius-lg` | 26px | Крупные панели, hero-изображение, форма записи |
| `--radius-pill` | 999px | Кнопки, пилюли-бейджи |
| `--radius-circle` | 50% | Аватары, узлы таймлайна |

Правило (против «одинаковые скруглённые прямоугольники везде»): крупные панели
получают `--radius-lg`, вложенные внутрь элементы — на ступень меньше
(`--radius-md` или `--radius-sm`), никогда не одинаковый радиус у контейнера и
его содержимого.

## Обводки

- `--stroke-hairline` (1px) — тонкие разделители, границы карточек.
- `--stroke-thin` (1.5px) — единая толщина всего SVG-иконочного набора (Этап 3) и
  акцентных обводок в интерфейсе — намеренное совпадение, связывает иконки с UI.
- `--stroke-bold` (3px) — линия таймлайна и другие «прочерченные» акценты.

## Тени

Все тени тонированы в оттенок `ink`/`moss` — нигде нет чистого чёрного
`rgba(0,0,0,*)`, что было бы дефолтом фреймворка.

| Токен | Значение | Где |
|---|---|---|
| `--shadow-sm` | `0 6px 16px rgba(34,86,77,.07)` | Карточки в состоянии покоя |
| `--shadow-md` | `0 16px 34px rgba(34,86,77,.12)` | Карточки/панели при hover, крупные блоки |
| `--shadow-lift` | `0 12px 26px rgba(14,110,91,.28)` | Тёплое свечение под основной CTA-кнопкой |

---

*Раздел «Фирменные приёмы» (надзаголовки, разделители секций, числа/статистика,
маркеры списков, поведение кнопок, подписи к фото, микро-текстура фона)
добавляется на Этапе 4, вместе с вёрсткой — см. порядок работы.*
